настроить порты в .env, config/dev.yaml и docker-compose.yml
выполнить комманду docker compose up -d
импортировать коллекцию в постман
go reform не получилось сгенерировать, застрял на 4 пункте, поэтому написал sql запросы.